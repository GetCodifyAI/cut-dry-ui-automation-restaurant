version: 2.1

executors:
  jmeter-executor:
    docker:
      - image: cimg/base:stable
    working_directory: ~/repo
    resource_class: xlarge

jobs:

  jmeter-test:
    executor: jmeter-executor
    steps:
      - checkout
      - run:
          name: Install Java 22
          command: |
            sudo apt-get update && sudo apt-get install -y wget
            wget https://download.java.net/java/GA/jdk22/830ec9fcccef480bb3e73fb7ecafe059/36/GPL/openjdk-22_linux-x64_bin.tar.gz
            sudo mkdir -p /usr/lib/jvm
            sudo tar zxvf openjdk-22_linux-x64_bin.tar.gz -C /usr/lib/jvm
            sudo update-alternatives --install /usr/bin/java java /usr/lib/jvm/jdk-22/bin/java 2200
            sudo update-alternatives --config java
            echo 'export PATH="/usr/lib/jvm/jdk-22/bin:$PATH"' >> $BASH_ENV
            source $BASH_ENV
            java -version
      - run:
          name: Install Maven
          command: |
            sudo apt-get install -y maven
            mvn -version
      - run:
          name: Install Chrome
          command: |
            wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
            sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
            sudo apt-get update
            sudo apt-get install -y google-chrome-stable
            google-chrome --version
      - run:
          name: Install ChromeDriver
          command: |
            # Install ChromeDriver version 135.0.7049.52
            CHROMEDRIVER_URL="https://storage.googleapis.com/chrome-for-testing-public/135.0.7049.52/linux64/chromedriver-linux64.zip"
            wget $CHROMEDRIVER_URL
            unzip chromedriver-linux64.zip
            sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
            sudo chmod +x /usr/local/bin/chromedriver
            chromedriver --version
      - run:
          name: Ensure results directory exists
          command: mkdir -p results
      - run:
          name: Install JMeter
          command: |
            wget https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.6.3.tgz
            tar -xvzf apache-jmeter-5.6.3.tgz
            export JMETER_HOME=$PWD/apache-jmeter-5.6.3
            export PATH=$JMETER_HOME/bin:$PATH
            echo "JMETER_HOME is set to $JMETER_HOME"
            echo "Listing contents of the JMeter bin directory:"
            ls -la $JMETER_HOME/bin
            if [ -f "$JMETER_HOME/bin/jmeter" ]; then
              echo "JMeter binary found!"
            else
              echo "JMeter binary not found!"
            fi
            jmeter -v
      - run:
          name: Verify ChromeDriver
          command: |
            which chromedriver
            chromedriver --version
#      - run:
#          name: Download PluginsManagerCMD.bat and PluginsManagerCMD.sh
#          command: |
#            mkdir -p apache-jmeter-5.6.3/bin
#            wget https://repo1.maven.org/maven2/kg/apc/jmeter-plugins-manager/1.10/PluginsManagerCMD.bat -O apache-jmeter-5.6.3/bin/PluginsManagerCMD.bat
#            wget https://repo1.maven.org/maven2/kg/apc/jmeter-plugins-manager/1.10/PluginsManagerCMD.sh -O apache-jmeter-5.6.3/bin/PluginsManagerCMD.sh
      - run:
          name: Install JMeter Plugin Manager
          command: |
            wget https://repo1.maven.org/maven2/kg/apc/jmeter-plugins-manager/1.10/jmeter-plugins-manager-1.10.jar -O apache-jmeter-5.6.3/lib/ext/JMeterPlugins-Manager.jar
            wget https://repo1.maven.org/maven2/kg/apc/cmdrunner/2.3/cmdrunner-2.3.jar -O apache-jmeter-5.6.3/lib/cmdrunner-2.3.jar
      - run:
          name: Verify files
          command: |
            ls -lh apache-jmeter-5.6.3/lib/ext/
            ls -lh apache-jmeter-5.6.3/lib/
            ls -lh apache-jmeter-5.6.3/bin/
            chmod -R 755 apache-jmeter-5.6.3/lib/ext/
#      - run:
#          name: Generate PluginsManagerCMD.jar
#          command: |
#            java -cp "apache-jmeter-5.6.3/lib/ext/JMeterPlugins-Manager.jar:apache-jmeter-5.6.3/lib/*" org.jmeterplugins.repository.PluginManagerCMDInstaller
##            ls -lh apache-jmeter-5.6.3/lib/ext/PluginsManagerCMD.jar
#      - run:
#          name: Install WebDriver Plugin
#          command: |
#            java -cp "apache-jmeter-5.6.3/lib/*:apache-jmeter-5.6.3/lib/ext/*" org.jmeterplugins.repository.PluginManagerCMD install jpgc-webdriver
      - run:
          name: Download and Install WebDriver Plugin
          command: |
            # Download the jpgc-webdriver plugin .zip
            wget https://jmeter-plugins.org/files/packages/jpgc-webdriver-4.13.0.0.zip -O jpgc-webdriver.zip
            
            # Unzip the package
            unzip jpgc-webdriver.zip -d apache-jmeter-5.6.3
            
            # Move all JAR files from the 'lib' folder into the JMeter's 'lib' folder
            mv apache-jmeter-5.6.3/lib/*.jar apache-jmeter-5.6.3/lib/
  
            # Move all JAR files from the 'lib/ext' folder into the JMeter's 'lib/ext' folder
            mv apache-jmeter-5.6.3/lib/ext/*.jar apache-jmeter-5.6.3/lib/ext/
      - run:
          name: Debug WebDriver
          command: |
            apache-jmeter-5.6.3/bin/jmeter -n -t src/test/jmeter-tests/jmeterWithSelenium.jmx -Jwebdriver.verbose=true
      - run:
          name: Run JMeter Test
          command: |            
            export JMETER_HOME=$PWD/apache-jmeter-5.6.3
            export PATH=$JMETER_HOME/bin:$PATH
            $JMETER_HOME/bin/jmeter -n -t src/test/jmeter-tests/jmeterWithSelenium.jmx -l results/test_results.jtl -e -o results/report -j jmeter.log -Jlog_level.jmeter=DEBUG
            cat jmeter.log



workflows:
  version: 2
  test:
    jobs:
      - jmeter-test
